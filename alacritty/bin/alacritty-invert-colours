#!/bin/sh
set -e

config="$XDG_CONFIG_HOME/alacritty/alacritty.toml"

if grep '/selenized-light.toml' "$config" ; then
    sed -i 's@/selenized-dark.toml@/selenized-light.toml@' "$config"
else
    sed -i 's@/selenized-light.toml@/selenized-dark.toml@' "$config"
fi
