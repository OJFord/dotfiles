#!/bin/sh
set -e

config="$XDG_CONFIG_HOME/alacritty/alacritty.yml"

if grep 'colors: \*darkscheme' "$config" >/dev/null; then
    sed -i 's/colors: \*darkscheme/colors: *lightscheme/' "$config"
else
    sed -i 's/colors: \*lightscheme/colors: *darkscheme/' "$config"
fi
