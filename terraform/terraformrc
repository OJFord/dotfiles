provider_installation {
  dev_overrides {
    "OJFord/android" = "${HOME}/code/terraform-provider-android"
    "OJFord/mullvad" = "${HOME}/code/terraform-provider-mullvad"
    "OJFord/wireguard" = "${HOME}/code/terraform-provider-wireguard"
    "amalucelli/nextdns"  = "${HOME}/code/terraform-provider-nextdns"
    "fly-apps/fly" = "${HOME}/code/terraform-provider-fly"
    "tailscale/tailscale" = "${HOME}/code/terraform-provider-tailscale"
    "terraform-routeros/routeros" = "${HOME}/code/terraform-provider-routeros"
  }

  direct {
  }
}
