[Unit]
Description=XDG Autostart (via dex)
After=default.target

[Service]
Type=oneshot
Environment=DISPLAY=:0
ExecStart=dex -a
Restart=on-failure
RemainAfterExit=yes

[Install]
WantedBy=default.target
