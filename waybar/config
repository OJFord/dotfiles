{
    "layer": "top",

    "modules-left": [
        "battery",
        "sway/workspaces",
        "sway/mode",
        "cpu",
        "memory",
        "disk",
        "custom/systemd-failed",
        "custom/systemd-user-failed",
        "custom/pkgupdates",
        "custom/gh-issues",
    ],
    "modules-center": [
        "clock",
    ],
    "modules-right": [
        "custom/mpris",
        "pulseaudio#sink",
        "pulseaudio#source",
        "idle_inhibitor",
        "tray",
    ],

    "battery": {
        "format-icons": ["", "", "", "", ""],
        "format-charging": "⚡ {capacity}%",
        "format-discharging": "{icon} {capacity}%",
        "format-not charging": "⚡",
    },

    "clock": {
        "format": "{:%d/%m %A %H:%M}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
    },

    "cpu": {
        "interval": 1,
        "format": "{icon0}{icon1}{icon2}{icon3}{icon4}{icon5}{icon6}{icon7} {usage:>2}%",
        "format-icons": ["▁", "▂", "▃", "▄", "▅", "▆", "▇", "█"],
    },

    "custom/gh-issues": {
        "exec": "bar-gh-issues",
        "on-click": "bar-gh-issues --open",
    },

    "custom/mpris": {
        "return-type": "json",
        "exec": "waybar-mpris --position --autofocus",
        "on-click": "waybar-mpris --send toggle",
        "on-click-right": "waybar-mpris --send player-next",
        "escape": true,
        "max-length": 64,
    },

    "custom/pkgupdates": {
        "exec": "bar-pkgupdates",
        "on-click": "bar-pkgupdates --open",
    },

    "custom/systemd-failed": {
        "format": "{} ",
        "interval": 120,
        "exec-if": "systemctl --failed --output=json | jq --exit-status 'length > 0' >/dev/null",
        "exec": "systemctl --failed --output=json | jq 'length'",
    },

    "custom/systemd-user-failed": {
        "format": "{} ",
        "interval": 120,
        "exec-if": "systemctl --user --failed --output=json | jq --exit-status 'length > 0' >/dev/null",
        "exec": "systemctl --user --failed --output=json | jq 'length'",
    },

    "disk": {
        "format": "{free}",
    },

    "idle_inhibitor": {
        "format": "{icon}",
        "format-icons": {
            "activated": "",
            "deactivated": "",
        },
        "tooltip-format-activated": "Idle inhibitor {status}",
        "tooltip-format-deactivated": "Idle inhibitor {status}",
        "timeout": 60,
    },

    "pulseaudio#sink": {
        "format": "{icon} {volume}% {desc}",
        "max-length": 32,
        "format-muted": "🔇 {volume}% {desc}",
        "format-icons": {
            "headphones": "",
            "default": ["", "", ""],
        },
        "on-click": "pulseaudio-control next-sink",
        "on-click-right": "pamixer --toggle-mute",
        // https://github.com/Alexays/Waybar/issues/1140
        "on-scroll-down": "pamixer --allow-boost --increase=1",
        "on-scroll-up": "pamixer --allow-boost --decrease=1",
    },

    "pulseaudio#source": {
        "format": "{format_source}",
        "format-source": " {volume}%",
        "format-source-muted": " {volume}%",
        "on-click-right": "pamixer --default-source --toggle-mute",
        // https://github.com/Alexays/Waybar/issues/1140
        "on-scroll-down": "pamixer --default-source --increase=1",
        "on-scroll-up": "pamixer --default-source --decrease=1",
    },

    "memory": {
    },

    "sway/workspaces": {
    },

    "sway/mode": {
    },

    "tray": {
    },
}
